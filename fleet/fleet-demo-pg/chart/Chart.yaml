apiVersion: v2
name: pg
description: >
  Minimal CNPG Postgres chart used to reproduce and fix the
  Rancher Fleet / ArgoCD GitOps drift issue caused by CNPG's
  one-time spec.bootstrap field (initdb vs recovery).
type: application
version: 0.1.0
appVersion: "1.0"
